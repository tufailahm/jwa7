import { HoverChangeColorDirective } from './hover-change-color.directive';

describe('HoverChangeColorDirective', () => {
  
  it('should create an instance', () => {
    const directive = new HoverChangeColorDirective();
    expect(directive).toBeTruthy();
  });
});
